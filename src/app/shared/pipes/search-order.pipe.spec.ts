import { SearchOrderPipe } from './search-order.pipe';

describe('SearchOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
